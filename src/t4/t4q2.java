package t4;

public class t4q2 {
    /*Correct the error for the following statements.

    a.  int sum=0;
        for (x = 10; x > 0; x++)
        sum += x;
    b.  do{
        x += 2;
        y += x;
        System.out.println(x + " and " + y);
        }while (x < 100);
    c.  for ( int x=1, y=20; x < y, x++, y-=2)
        System.out.println(x + " " + y);
    d.  int i =1;
        while(i<10) {
            i++;
            if (i==10)
            System.out.println("Program End");
        }

     */
}
